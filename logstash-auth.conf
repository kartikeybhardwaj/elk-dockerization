input {
  tcp {
    port => 9600
    codec => json
  }
}
output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    user => "elastic"
    password => "<ELASTIC_PASSWORD>"
  }
  stdout { codec => rubydebug }
}
